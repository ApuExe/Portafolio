# V1.1 RC4 — Light Blend QA

## Objetivo

Eliminar el cambio abrupto entre la escena oscura de `SIGNAL → CLARITY` y el Hero claro, haciendo que ambos estados se solapen visualmente.

## Implementación

- Fase explícita `intro-blending` antes de `intro-resolved`.
- Scrim cinematográfico con fade de 880 ms.
- Superficie de intro animada por Web Animations API a través de:
  - `#07090d`
  - `#171b22`
  - `#737b87`
  - `#d8dde5`
  - `#f4f5f8`
- Reveal del Hero solapado con la transición de luz.
- Session key actualizada a `ls-v11-signal-clarity-rc4-played`.

## Verificación

- `pytest -q`: 33 passed.
- `python tests/validate_v213.py`: PASS.
- `node --check`: `experience-intro.js`, `app.js` y `motion-system.js` sin errores.
- CSS parseado con `tinycss2`: 0 errores.
- Chromium headless: progresión visual observada de oscuro → gris frío → claro, con el Hero visible antes de finalizar el blend y sin `pageerror`.
- Reduced motion y controles de escape/skip preservados.
